
<div class="height d-flex justify-content-center align-items-center">
    
    <div class="card p-3 d-flex align-content-center">
        
        <div class="d-flex justify-content-between align-items-center">
            <div class="mt-2">
                <h4 class="text-uppercase">{{products.0.brand}}</h4>
                <div class="mt-5">
                    <h5 class="text-uppercase mb-0">{{products.0.category}}</h5>
                    <h1 class="main-heading mt-0">{{products.0.title}}</h1>
                    {{!-- <div class="d-flex flex-row user-ratings">
                        <div class="ratings">
                        <i class="fa fa-star"></i>
                        <i class="fa fa-star"></i>
                        <i class="fa fa-star"></i>
                        <i class="fa fa-star"></i>
                        </div>
                        <h6 class="text-muted ml-1">4/5</h6>
                    </div> --}}
                </div>
            </div>
            <div class="image">
                <img src="{{products.0.thumbnails.[0]}}" width="200">
            </div>
        </div>
        
        {{!-- <div class="d-flex justify-content-between align-items-center mt-2 mb-2">
            <span>Available colors</span>
            <div class="colors">
                <span></span>
                <span></span>
                <span></span>
                <span></span>
            </div>
            
        </div> --}}
        
        
        <p>{{products.0.description}}</p>

        <h6 class="text-uppercase justify-item-center">Stock: {{products.0.stock}}</h6>

        <div class="d-flex justify-content-center">

            <h4 class="text-uppercase">$ {{products.0.price}}</h4>
        </div>
        
        <button class="btn btn-danger">Agregar al Carrito</button>
    </div>
    
</div>